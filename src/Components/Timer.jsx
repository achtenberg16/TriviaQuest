import React from 'react';
// import { INITIAL_TIME } from '../helpers/constants';

function Timer() {
//   const [time, setTime] = useState(INITIAL_TIME);

  //   useEffect(() => {
  //     const interval = setInterval(() => {
  //       setTime(time - 1);
  //     }, 1000);
  //     return () => clearInterval(interval);
  //   }, []);
  return (
    <p>ola</p>
  );
}

export default Timer;
